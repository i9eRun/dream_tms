spring.application.name=tms
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@190.150.10.207:1521/TMS
spring.datasource.username=TMS_WMS
spring.datasource.password=TMS_WMS_123

# CORS 정책 때문에 외부에서 접속가능하게 하기위해
server.address=0.0.0.0
server.port=8080

# 개발시 http 환경에서 세션 사용하기 위한 설정
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=none

# JPA 설정 (validate | update | none | create)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
spring.jpa.open-in-view=false

#snake_case → camelCase로 변환설정
#spring.jackson.property-naming-strategy=SNAKE_CASE